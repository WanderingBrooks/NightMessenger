// NOTE: Since backlog-com-website repo uses older JS, the prettier configuration here is DIFFERENT than in other repos
module.exports = {
  arrowParens: 'always',
  printWidth: 80,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  useTabs: false,
  tabWidth: 2,
}